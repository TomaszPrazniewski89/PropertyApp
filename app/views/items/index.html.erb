<% @items.each do |item| %>



<div class="container">

    <%=link_to image_tag(item.image.url, size: "50x50"), type_category_item_path(id: item.id) %>


  <p class="a"><%= link_to item.title, type_category_item_path(id: item.id) %></p>

</div>

<% end %>
